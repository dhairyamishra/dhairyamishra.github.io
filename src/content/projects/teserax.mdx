---
title: "Teserax.io — Agentic Knowledge-Graph Application"
description: "Built from 0 to 1 as a live deployed agentic knowledge-graph application. A DAG-safe agentic workflow engine auto-orchestrates tool-based pipelines on a React Flow canvas. Brave Search Auto-RAG grounds every response in real-time cited sources. Multi-model support with fallback routing between local and cloud models, MCP integration for tool connectivity, Hono/TypeScript backend with Zod validation, 226 Vitest tests and Playwright E2E coverage — deployed on EC2 and Cloudflare."
date: "2026-02"
tags: ["React", "React Flow", "Hono", "Zustand", "Supabase", "LLM", "GPT-4", "Grok", "Playwright", "Docker", "MCP", "Brave Search"]
featured: true
category: "full-stack"
github: "https://github.com/dhairyamishra/teserax"
liveUrl: "https://www.teserax.io"
metric: "v2.0"
metricLabel: "Shipped"
---

import VideoEmbed from '../../components/mdx/VideoEmbed.astro';

<VideoEmbed
  src="https://www.youtube.com/watch?v=aI9RDIcJBQ0"
  title="Teserax demo"
  caption="Teserax v2.0 — graph-based AI thinking tool walkthrough"
  width="wide"
/>

## Overview

Teserax is an **agentic knowledge-graph application** built from 0 to 1 as a fully live, deployed product. It transforms linear LLM chat into a visual, non-linear exploration space — users ask questions in a persistent chat lane, watch responses decompose into structured graph nodes on a React Flow canvas, branch into parallel threads, merge insights, cross-link between reasoning paths, and summarize — all with cloud persistence, multi-model LLM support, and bring-your-own-key for any provider.

The application matured through five development phases and a post-phase monetization sprint into a **dual-lane, chat-first exploration tool** with a versioned prompt registry, DAG-safe crosslink architecture, glassmorphism UI, deterministic tree layout with swim lanes, and production hosting on EC2 behind Cloudflare. At its core is a **DAG-safe agentic workflow engine** that auto-orchestrates tool-based pipelines from natural language — decomposing queries into multi-step execution plans and running them in parallel with dependency tracking. **Brave Search Auto-RAG** grounds every LLM call in real-time web retrieval, with results deduplicated, cited, and injected as structured XML context. **MCP integration** provides extensible tool connectivity. 64 issues closed, 1 deferred (real-time CRDT collaboration).

## Key Features

- **Dual-Lane UI**: Three-panel layout — ChatPanel (persistent conversation, auto-analyze toggle), GraphCanvas (React Flow canvas as the visual workflow interface with swim lanes, crosslinks, viewport virtualization), and a slim Context Strip sidebar with ancestry chain and quick actions
- **DAG-Safe Agentic Workflow Engine**: LLM planner decomposes any natural-language query into a 3–7 step structured execution plan, then a DAG pipeline engine auto-orchestrates all tool-based pipeline steps in parallel with dependency tracking and per-step progress visualization
- **Brave Search Auto-RAG**: Every LLM call is automatically augmented with real-time web retrieval — the prompt is distilled into 3–4 parallel Brave Search queries, results are deduplicated and injected as cited XML context, grounding responses in live sources with exponential backoff retry
- **Multi-Model Support with Fallback Routing**: OpenAI (GPT-4o, GPT-4o-mini) and Grok/xAI (Grok-4.1, Grok-3, Grok-3-mini) via a universal `OpenAICompatAdapter`, with fallback routing between local and cloud models on validation or connection failure, plus bring-your-own-key for 8+ providers with AES-256-GCM encryption at rest
- **MCP Integration**: Model Context Protocol support for extensible tool connectivity, enabling external tool invocation within agentic pipelines
- **Node Chat Modal**: Click any node to open a streaming chat modal for in-place iteration — apply changes to the node or spawn a child node, with ancestry-aware conversation context, file attachments (PDF/image/text), and integrated Brave Search Auto-RAG
- **Four-Handle Crosslinks**: DAG-safe architecture with structural edges (top/bottom, DFS cycle detection) and non-structural annotation crosslinks (left/right side tabs) with type, strength, and auto-detection via tag/topic Jaccard overlap
- **Prompt Registry**: 19 versioned templates with `renderTemplate()` interpolation, canonical Zod schemas for structured output, corrective retry that feeds validation errors back to the LLM, and crosslink context injection into prompts with token budgeting
- **Glassmorphism Design**: `backdrop-blur` nodes with dynamic sizing, branch-colored borders, compact operator pills, shimmer skeletons, and hover-expand — plus LLM-generated titles and summaries via a debounced background sidecar
- **Cloud Persistence**: Supabase PostgreSQL with 11 RLS-protected tables, email/Google OAuth, dashboard with search and templates, `Ctrl+S` cloud save, named version history snapshots, and shareable read-only links
- **Pipeline Templates**: 3 auto-executing templates — Financial Analysis (6-step bull/bear framework), Pros & Cons (3-step balanced assessment), and Decision Matrix (4-step comparison merge)
- **Chunk Review**: Accept, reject, or inline-edit individual response chunks (9 typed chunk kinds: claim, step, example, definition, table, code, data_point, question, analysis) before they join the graph

## Architecture

Teserax uses a **dual-lane response model**. Every LLM operation emits immutable chat messages (Lane A) in parallel with graph nodes (Lane B), linked via `projectedNodeIds`. The graph stores both raw provider responses and derived `StructuredProjection` data (Zod-validated typed blocks).

**Core loop**: user sends a message in the ChatPanel → LLM responds via SSE streaming (server key or BYOK) → response is projected into a `StructuredProjection` with typed blocks → each block becomes a node on the graph canvas with structural DAG edges → user can branch (new swim lane thread), merge (converge), summarize (compress), or cross-link (annotate) — with full context lineage and token budget management trimming oldest messages at 80% capacity.

The AI orchestrator adds a second path: when auto-analyze is enabled, the `orchestrator.plan.v1` prompt asks the LLM to decompose the query into a multi-step JSON plan, then `templatePipeline.ts` executes it as a DAG with dependency-aware parallel step execution and a fallback 3-step plan on planner failure.

## Infrastructure & Quality

- **API Server**: Hono framework (TypeScript) proxying LLM calls (keys stay server-side), with Zod-validated structured output, ES256/HS256 JWT auth, sliding-window rate limiting (100 req/hr), CSP/HSTS security headers, audit logging, and 5MB body / 100k character input limits
- **Hosting**: Deployed on EC2 behind Cloudflare with Origin Certificate SSL, Nginx TLS 1.2/1.3, real IP restoration, Docker multi-stage builds, and a 7-step SSH deploy pipeline with swap management and health checks
- **Testing**: 226 Vitest tests across 22 files (API, component, prompt, stress, golden set) and Playwright end-to-end spec files covering a11y, auth, BYOK, canvas interaction, cloud persistence, export/import, keyboard shortcuts, and more — with mock LLM server, Stripe/Supabase stubs, and JWT factory
- **CI/CD**: GitHub Actions quality gate (lint, typecheck, build, unit tests, E2E, security audit) plus deploy workflow with Cloudflare Origin Cert install and concurrency groups
- **Observability**: In-memory execution trace collector, metrics API, Sentry error tracking (frontend + backend), and privacy-respecting Plausible analytics

## Tech Stack

- **Frontend**: React 18, React Flow v12 (`@xyflow/react`), Zustand + Immer, Tailwind CSS + shadcn/ui, Framer Motion, react-markdown + remark-gfm, cmdk, sonner
- **Backend**: Hono (Node.js/TypeScript), OpenAI SDK + xAI/Grok via `OpenAICompatAdapter`, Zod (validation + structured output schemas), tiktoken, Drizzle ORM, Supabase PostgreSQL, AES-256-GCM encryption
- **Integrations**: Brave Search API (Auto-RAG), MCP (tool connectivity), Grok Imagine (image generation), pdfjs-dist (client-side PDF parsing)
- **Testing**: Vitest (226 unit/integration tests across 22 files), Playwright (multi-browser E2E + mobile)
- **Infra**: Docker + Docker Compose, Nginx, Cloudflare, EC2, GitHub Actions CI/CD, Supabase Auth + RLS
